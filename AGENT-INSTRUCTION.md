# AGENT-INSTRUCTION

PROR.CO 웹사이트 저장소 작업 지침. 코딩 에이전트(Claude Code / Cursor 등)에게 이 파일을 먼저 읽히세요.

## 1. 프로젝트 구조

정적 HTML 사이트입니다. 빌드 단계, 서버 코드, 패키지 매니저가 없습니다. 파일을 그대로 정적 호스팅(Vercel)에 올립니다.

```
PRORCO 홈페이지.dc.html      메인
시공패키지.dc.html            패키지 개요
간접조명패키지.dc.html
우물천장패키지.dc.html
우물천장실링팬패키지.dc.html
실링팬패키지.dc.html
개별항목.dc.html              항목별 단품 가격
색상안내.dc.html              색온도 안내
상세견적.dc.html              셀프견적 (실시간 합산)
시공사례.dc.html
시공후기.dc.html
고객센터.dc.html
ReviewStrip.dc.html          패키지 페이지 하단 후기 구간 (공용)
mobile-nav.js                모바일 햄버거 내비게이션 (전 페이지 공용)
image-slot.js                사진 드롭 슬롯 컴포넌트
image-slots.state.json       슬롯 id → 사진 파일 매핑
support.js                   런타임 (수정 금지)
assets/                      사진, 영상, 폰트
assets/slots/                슬롯에 등록된 사진
```

### 작업 규칙

- 각 `.dc.html`은 브라우저에서 바로 열리는 독립 파일입니다. 스타일은 전부 인라인이며, 클래스 기반 CSS를 도입하지 마세요.
- `support.js`는 런타임입니다. 절대 수정하지 마세요.
- 내비게이션 순서·문구를 바꿀 때는 **전 페이지 + `mobile-nav.js`** 를 함께 고쳐야 합니다. 현재 순서: 시공패키지 / 셀프견적(브랜드 컬러, 굵게) / 개별항목 / 색상안내 / 시공사례 / 시공후기 / 고객센터.
- 후기 구간은 `ReviewStrip.dc.html` 한 곳에서 관리됩니다. 패키지 5개 페이지가 이 파일을 불러 쓰므로 여기만 고치면 전부 반영됩니다.
- 사진은 `image-slots.state.json`의 슬롯 id로 연결됩니다. 같은 id를 쓰면 여러 페이지에 같은 사진이 나옵니다. 사진을 교체할 때는 `assets/slots/` 안의 **파일명을 유지**하고 내용만 바꾸세요.
- 반응형 기준점: 1180px / 1024px / 720px. 720px 이하가 모바일 레이아웃입니다.

### 알려진 이슈

`assets/slots/` 사진 다수가 500~600px 축소본(7~20KB)입니다. 표시 폭 대비 해상도가 부족해 흐리게 보입니다. 원본을 가로 1600px, 품질 80% 정도로 다시 넣으면 해결됩니다.

## 2. Aeolo 콘텐츠 피드 연동 (미완료)

전체 가이드: https://aeolo.io/integrate/content-feed.md — 작업 전에 반드시 읽으세요.

### 현재 막힌 이유

가이드의 필수 조건 두 가지를 이 저장소는 충족할 수 없습니다.

1. **서버 렌더링 필수.** 지금은 정적 파일뿐이라 피드를 서버에서 받아 HTML에 넣을 수 없습니다. 브라우저 JS로 그리는 방식은 가이드가 금지합니다(AI 크롤러에 보이지 않음).
2. **키는 서버 전용 환경변수.** 서버가 없으므로 키를 안전하게 둘 곳이 없습니다. 정적 파일에 넣으면 공개됩니다.

추가로 이 사이트에는 블로그 목록/상세 라우트와 sitemap이 없습니다.

### 진행 방향 (미결정 — 발주자 확인 필요)

- **A. Next.js(App Router)로 전환** — 기존 페이지를 옮기고 `/blog` 목록·상세 라우트와 sitemap을 만듭니다.
- **B. 블로그만 분리** — 정적 사이트는 유지하고 `/blog` 경로만 서버 렌더링 프로젝트로 붙입니다.
- **C. Aeolo 호스팅 블로그** — 코드 작업 없음. 단, 권위가 우리 도메인에 쌓이지 않습니다.

### A 또는 B를 택했을 때 반드시 지킬 것

- 피드 호출은 **서버 측에서만**. 키는 `AEOLO_KEY` 환경변수(서버 전용), 저장소에 커밋 금지.
- `?base=https://<우리도메인>/blog` 를 붙여 canonical이 우리 도메인을 가리키게 합니다. base 경로는 실제 글 주소와 일치해야 합니다.
- **페이지네이션 필수.** 한 응답은 최대 50건입니다. `next_url`이 없어질 때까지 따라가세요. 한 번만 받으면 나머지 글이 사이트에 존재하지 않게 됩니다.
- **로케일 판별.** 번역본은 각각 별개 항목입니다. `_geo.family_id`로 묶고 우리가 서비스하는 언어만 노출하세요. 같은 글이 언어마다 중복 카드로 나오는 것이 가장 흔한 실수입니다.
- `hreflang`은 `_geo.alternates` 값을 **그대로** 씁니다. `?lang=` 같은 우리 규칙으로 URL을 만들지 마세요.
- `canonical`은 `_geo.canonical` 문자열 그대로. 파라미터를 덧붙이지 마세요.
- `content_html`은 HTML입니다. 마크다운 렌더러에 그대로 넣으면 태그가 글자로 보입니다. 없을 수도 있으니 `item.content_html ?? ''`로 읽으세요.
- `image`는 빈 문자열로 오는 경우가 많습니다. `item.image?.trim() || null`로 걸러야 깨진 이미지가 안 생깁니다.
- 항목별로 방어적으로 매핑하세요. 한 건이 잘못돼도 목록 전체가 사라지면 안 됩니다.
- **sitemap에 추가.** 이 단계를 빼면 글이 잘 보이는데도 크롤러가 존재를 모릅니다. 로케일별 URL을 모두 넣고 `lastmod`는 `date_modified`를 씁니다. 빌드 타임 sitemap이면 배포할 때까지 갱신되지 않으니 요청 시 생성으로 바꾸세요.
- 피드 캐시는 60초. `ETag`를 `If-None-Match`로 되돌려 보내면 비용이 거의 없습니다.
- 소유권 확인 태그 `<meta name="aeolo-site-verification" content="aeo-…">` 는 **루트 레이아웃의 `<head>` 마크업**에 넣으세요. `meta()` 함수에 넣으면 자체 메타를 정의한 라우트에서 사라집니다.

### 필요한 값 (발주자에게 받아야 함 — 에이전트가 스스로 구할 수 없음)

- 피드 키 `atok_…` → 환경변수 `AEOLO_KEY`
- 소유권 확인 토큰 `aeo-…` → 루트 head의 meta 태그
- 사이트 origin과 글이 실제로 놓일 경로

### 완료 판정

가이드 §6의 curl 점검을 그대로 실행하고 결과를 보고하세요. 렌더링 여부만 보지 말고 canonical·hreflang·`<html lang>`이 무엇을 주장하는지 확인해야 합니다.
